query GetKanbanTasks($name: String) {
  todoTasks: tasks(input: { status: TODO, name: $name }) {
    ...TaskFields
  }
  inProgressTasks: tasks(input: { status: IN_PROGRESS, name: $name }) {
    ...TaskFields
  }
  doneTasks: tasks(input: { status: DONE, name: $name }) {
    ...TaskFields
  }
}
