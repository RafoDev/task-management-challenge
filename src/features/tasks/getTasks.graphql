query GetTasks($status: Status!) {
  tasks(input: { status: $status }) {
    id
    name
    status
    dueDate
    pointEstimate
    tags
    position
    assignee {
      id
      fullName
      avatar
    }
    creator {
      id
      fullName
    }
    createdAt
  }
}
