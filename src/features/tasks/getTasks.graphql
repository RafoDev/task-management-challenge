query GetTasks {
  tasks(input: {}) {
    id
    name
    status
    dueDate
    pointEstimate
    tags
    position
    assignee {
      id
      fullName
    }
    creator {
      id
      fullName
    }
    createdAt
  }
}
